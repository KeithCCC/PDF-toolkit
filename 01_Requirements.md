# PDF Toolkit 要件定義書

- バージョン: 1.0
- 作成日: 2026-09-20
- 状態: 初期版の要件として承認済み
- 対象: Windows Store向けBasic版
- 保存先: 20_Projects / Windowsストアアプリ / 05_PDF Toolkit

## 1. 目的と位置付け

PDFのページ整理、シンプルなコメント、Word・Excelとの形式変換を行うWindowsアプリを開発する。

中心となる体験は、ページの並べ替え・向きの変更・結合・分割を、仕上がりを見ながら直感的に操作できることである。画面上で確認したページの順番・向き・構成を、そのまま出力PDFに反映する。

本書は、ユーザーが個別に承認した設計と、細かな設計への包括承認に基づいて採用した提案をまとめたもの。市場調査で紹介した他製品の機能すべてを実装対象とするものではない。

## 2. 開発の共通方針

- Rust + Tauriを基本技術スタックとする。
- まずLLMやAPIキーなしで利用できるBasic版を完成させ、Windows Storeでリリースする。
- Basic機能はローカルで処理し、アプリ独自のアカウントや文書のクラウドアップロードを不要とする。
- Word・ExcelからPDFへの変換のみ、対応するデスクトップ版Microsoft Officeに依存する。
- Basic版リリース後のBYOKによるAI拡張は将来フェーズとし、PDF Toolkit固有のAI機能は本書では定義しない。
- 実行管理はTrello「Mission Control」を正本とし、開発用Codexマスタープロンプトは別途作成する。

## 3. 対応ファイル

| 用途 | 初期対応 |
|---|---|
| PDFの取り込み・編集・出力 | PDF |
| Wordとの相互変換 | DOCX |
| Excelとの相互変換 | XLSX |
| 作業の保存・再開 | PDFのコピーと編集情報を含む単一のプロジェクトファイル |

旧形式のDOC・XLS、およびマクロ付きOffice形式は初期対象外とする。プロジェクトファイルの具体的な内部形式と拡張子は実装設計時に決める。

## 4. WYSIWYG画面と操作

### 4.1 主画面

- ページのサムネイル一覧を主画面とする。
- 画面上の1グループを、保存される1つの出力PDFに対応させる。
- 各グループに出力ファイル名とページ数を表示する。
- 各ページの出力順、元ファイル名、元ページ番号を確認できるようにする。
- サムネイルの大きさを調整できるようにする。
- 回転によって縦長・横長が変わった場合も、表示に即時反映する。
- ページをダブルクリックすると拡大プレビューを開く。
- 拡大プレビューで内容を確認し、コメントを編集できるようにする。

### 4.2 選択と操作

- クリックで1ページを選択する。
- Ctrl + クリックで複数ページを選択する。
- Shift + クリックで範囲を選択する。
- ドラッグ＆ドロップでページを並べ替える。
- ドラッグ中は挿入位置を表示する。
- グループ間の通常ドラッグは移動とする。
- Ctrl + ドラッグはコピーとする。
- ボタンからも移動・コピーを選択できるようにする。
- 複数ページをまとめて移動・コピー・回転・削除できるようにする。
- 編集操作の「元に戻す」「やり直す」に対応する。
- ページ削除のたびには確認を表示せず、「元に戻す」で復旧できるようにする。

## 5. PDFの取り込み

- ファイル選択とドラッグ＆ドロップの両方に対応する。
- 複数のPDFを一度に取り込めるようにする。
- 取り込んだ直後は、元PDFごとに出力グループを作成する。
- 初期のグループ名には元ファイル名を使用する。
- 開くためのパスワードが必要なPDFでは、パスワード入力に対応する。
- 扱えない保護設定の場合は、理由を表示して取り込みを止める。

## 6. ページ整理・結合・分割

### 6.1 並べ替え・回転・削除

- 同じグループ内およびグループ間でページを並べ替えられるようにする。
- ページごとに左90度・右90度の回転操作を提供する。
- 回転の繰り返しで180度の向き修正にも対応する。
- 複数ページの一括回転に対応する。
- 不要なページを削除できるようにする。
- 変更後の順番と向きを即時にプレビューへ反映し、保存結果にも適用する。

### 6.2 結合

- 複数のPDFを1つの出力グループにまとめられるようにする。
- グループ全体を別のグループへドラッグして結合できるようにする。
- 異なるPDFの必要なページだけを選び、任意の順番で組み合わせられるようにする。
- 結合後もページ単位で並べ替え・回転・削除できるようにする。

### 6.3 分割・抽出

- ページとページの間に区切りを入れ、別の出力グループに分割できるようにする。
- 選択したページを新しいグループへ移して分割できるようにする。
- 選択ページをコピーして新しいグループを作り、元のグループにも残せるようにする。
- 指定したページや範囲を別のPDFとして保存できるようにする。
- 保存前に、各出力PDFのファイル名・ページ数・順番・向きを確認できるようにする。

## 7. コメント

### 7.1 追加と編集

- 元のPDF本文を変更せず、ページ上に文字コメントをオーバーレイで追加する。
- コメントは、文字が常に見えるテキストボックス方式とする。
- 任意の位置への配置、移動、サイズ変更、文字編集、削除に対応する。
- 文字サイズ、文字色、背景色を変更できるようにする。
- 初期値は読みやすい文字サイズ、黒文字、淡い背景色とする。
- ページの移動・コピーにコメントも追従する。
- ページを回転するとコメントもページと一緒に回転する。

### 7.2 保存と互換性

- コメントをページ画像に焼き付けず、このアプリで再選択・再編集・削除できる状態でPDFに保存する。
- 他のPDFビューアでも表示できる形式を使用する。
- 保存したPDFの表示と印刷にコメントを反映する。
- 他のビューアでの編集操作は、そのビューアの対応に依存する。
- 他のアプリで付けられた既存注釈は、表示・保持を基本とする。
- 初期版で再編集を保証するのは、このアプリで作成した文字コメントとする。

## 8. 形式変換

### 8.1 共通方針

- PDFとWord、PDFとExcelの相互変換に対応する。
- 変換後の編集・再利用よりも、元の見た目の再現を優先する。
- PDFからOffice形式への変換では、文章や表の構造を復元せず、各ページを画像として配置する。
- 保存前に実際の変換結果をプレビューで確認できるようにする。

### 8.2 PDF → Word

- PDFの1ページを画像化し、Wordの1ページに配置する。
- 元ページの縦横比を維持する。
- 文字や表は画像として扱い、文字編集・文字検索可能な文書への復元は行わない。
- DOCXファイルの生成にMicrosoft Wordのインストールを必須としない。

### 8.3 PDF → Excel

- PDFの1ページを画像化し、Excelの1シートに配置する。
- 元ページの縦横比を維持する。
- 印刷時も各シートが1ページに収まる設定にする。
- 表をセルとして復元せず、セル編集・計算可能な表への変換は行わない。
- XLSXファイルの生成にMicrosoft Excelのインストールを必須としない。

### 8.4 画像配置の画質

- 「標準」「高画質」を用意する。
- 初期値は「標準」とする。
- 具体的な解像度は、実際の出力品質とファイル容量を検証して決める。

### 8.5 Word → PDF

- PCにインストールされたデスクトップ版Microsoft Wordを利用する。
- 文書のレイアウトを反映してPDF化する。
- 初期版は文書全体を変換する。
- 必要なページの抽出は、変換後のページ整理で行う。
- Wordが利用できない場合は機能をグレーアウトし、必要なアプリが利用できない旨を表示する。

### 8.6 Excel → PDF

- PCにインストールされたデスクトップ版Microsoft Excelを利用する。
- 変換前にシート一覧を表示する。
- 全シート、または選択したシートを出力できるようにする。
- 各シートの印刷範囲、用紙サイズ、向き、改ページ設定を反映する。
- 結果をプレビューしてから保存する。
- Excelが利用できない場合は機能をグレーアウトし、必要なアプリが利用できない旨を表示する。

### 8.7 Office連携の境界

- Officeがない場合の代替変換エンジンは導入しない。
- Officeの有無による制限は、対応するOfficeファイルからPDFへの変換に限定する。
- ページ整理、コメント、PDFから画像配置によるDOCX・XLSX出力は、Officeなしでも利用可能とする。
- Officeファイルを作業画面に取り込む場合は、PDF化してプレビューを表示し、確認後に出力グループとして追加する。
- 作業プロジェクトには変換済みPDFを保存し、元のOfficeファイルとの連動更新は行わない。
- 変換に失敗した場合は、対象ファイルと理由を表示し、ほかの作業を継続できるようにする。
- Officeやフォントの環境差を含めた再現性の検証は必要であり、あらゆる文書の完全一致を保証する要件ではない。

## 9. PDFの書き出し

- 元ファイルは変更せず、編集結果を新しいPDFとして保存する。
- 出力グループごとに1つのPDFを作成する。
- グループ名を出力ファイル名として使用する。
- 保存前に出力ファイル名を変更できるようにする。
- 全グループ、または選択したグループを指定フォルダーへまとめて書き出せるようにする。
- 出力ファイル名の重複を確認する。
- 保存先に同名ファイルがある場合は、上書き前に確認する。
- 画面で確認した順番・向き・グループ構成・コメントを出力に反映する。
- 自動保存とは別に、明示的な書き出し操作で出力PDFを作成する。

## 10. 作業プロジェクト

### 10.1 手動保存・再開

- ページの並び、回転、出力グループ、コメントを作業状態として保存する。
- 取り込んだPDFのコピーと編集情報を、1つのプロジェクトファイルにまとめる。
- 元PDFを移動・削除しても、プロジェクト単体で作業を再開できるようにする。
- 別のPCへプロジェクトを移して作業を再開できるようにする。
- PDFのコピーを含めるため、保存容量が増えることを設計上の前提とする。
- 作業プロジェクトの保存と、完成したPDFの書き出しを区別する。

### 10.2 自動保存・復元

- 手動保存に加え、編集中の状態を復旧用に自動保存する。
- アプリの異常終了後、次回起動時に復旧可能な作業状態を提示する。
- ユーザーが復元または破棄を選べるようにする。
- 自動保存では出力PDFを書き換えない。

## 11. 応答性とエラー処理

- 時間のかかる取り込み・変換・書き出しでは進捗を表示する。
- 長時間の処理をキャンセルできるようにする。
- 取り扱えないPDFやOffice変換の失敗では、対象ファイルと理由を表示する。
- 個別ファイルの失敗が、ほかの作業を継続できなくする設計を避ける。

## 12. 初期版の対象外

- 署名機能。
- 凝ったコメント管理、承認フロー、共同レビュー。
- PDF内の既存本文・画像を直接書き換える本格的な編集機能。
- PDFから編集可能な文章・表・セル構造を復元する変換。
- OCR。
- Officeがない環境でのWord・ExcelからPDFへの代替変換。
- DOC・XLSなどの旧Office形式、マクロ付きOffice形式。
- 元のOfficeファイルとの連動更新。
- クラウド共有・アプリ独自アカウント。
- BYOKによるAI機能の実装。
- 圧縮、透かし、ページ番号追加、墨消し、図面計測など、市場調査で挙げたが今回採用していない機能。

## 13. 要件に対応する確認観点

以下は、本書の要件が満たされたことを確認するための観点であり、検証済みという意味ではない。

- 複数PDFを取り込み、ページを目で確認しながら並べ替え・回転・結合・分割できる。
- 複数選択、通常ドラッグによる移動、Ctrl + ドラッグによるコピー、元に戻す・やり直すが機能する。
- 書き出したPDFの順番・向き・構成がプレビューと一致し、元PDFが維持される。
- 文字コメントを追加・編集して保存でき、再度開いたPDFでもこのアプリで再編集できる。
- コメントが他の対応ビューアでも表示され、印刷に反映される。
- PDFからDOCX・XLSXへ、ページ画像を配置する方式で変換できる。
- WordとExcelの利用可否を個別に判定し、利用できない変換機能をグレーアウトする。
- Office利用時の変換結果を確認でき、Excelでは出力シートを選択できる。
- 元PDFが元の場所になくても、保存したプロジェクトから作業を再開できる。
- 異常終了後に自動保存から復元するか破棄するか選択できる。
- 同名ファイルの上書き確認、処理進捗、キャンセル、ファイル単位のエラー表示が機能する。

## 14. 実装設計で決める事項

機能方針は本書を基準とし、以下の具体値・実装方式は別途決める。

- PDF表示・編集・注釈に使用するライブラリと、配布可能なライセンス構成。
- Officeの対応バージョン、利用可否の判定方法、連携方式。
- プロジェクトファイルの内部形式・拡張子・形式バージョン管理。
- 自動保存の間隔と保持方法。
- 画像変換の標準・高画質の解像度。
- 大容量ファイルの検証条件、性能目標、互換性の検証対象。

重大でない詳細は推奨案を採用して進める。有料ライブラリなどの費用発生、文書の外部送信、主要機能の削減、元ファイルを失う可能性のある仕様変更は、個別にユーザーへ確認する。

## 15. 関連資料

- [2026 Product Development Roadmap](https://drive.google.com/file/d/1MOZlm5y5U6Mo1ZIzuq3EJ-W9Xc5Iw7ZW/view)
- [PDF Toolkit README](https://drive.google.com/file/d/1Puvb6LudZR30tKXqpYMag6e4d2mx8jIT/view)

