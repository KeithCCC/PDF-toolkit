# PDF Toolkit — Codex向け開発マスタープロンプト

> 状態：開発着手済み（2026-09-20）。`refinement.md` は所在未確認のため未反映。ユーザーの「実装を進めて」に基づき、現在の要件で実装を進める。
> このファイルは開発を依頼する際にCodexへ渡す指示書です。

---

あなたは、このリポジトリのPDF Toolkitを設計・実装・検証する開発担当です。以下の指示とプロジェクトの要件資料に基づき、Windowsで利用できるスタンドアロンのデスクトップアプリケーションを完成させてください。計画や画面モックだけで完了せず、実ファイルを処理できる実装と配布ビルドまで進めてください。

## 1. 最初に確認する資料と優先順位

1. 作業ディレクトリ、Gitの状態、適用される `AGENTS.md`、既存のコード・設定を確認する。既存の変更を失わない。
2. `01_Requirements.md`、`refinement.md`、`README.md` を全文読む。
3. `refinement.md` がない場合は、読んだと扱わず所在を確認する。調査・要件整理は進めてよいが、補足要件を確認するまで仕様を確定したとして実装を開始しない。ユーザーが欠落資料なしでの着手を明示的に認めた場合は、その前提を記録する。
4. 最新のユーザー指示を優先し、機能要件は `01_Requirements.md` を基準とする。`refinement.md` は補足として照合し、明示的な変更指示がある場合だけ置き換える。重大な矛盾はユーザーに確認する。
5. 本プロンプトの技術的な初期案は既存要件と区別する。初期案より妥当な方法があれば根拠を記録して採用してよい。

## 2. 今回のゴール

- RustとTauriを使用し、Windowsで動作するBasic版を完成させる。
- PDF処理、編集状態、ファイル入出力、保存、変換の主要処理はRust側に実装する。
- スタンドアロンとは、開発用サーバー、Node.js、Rustツールチェーン、独自のバックエンドサービスを利用者が起動・導入しなくても、インストール済みアプリとして使用できることを意味する。単一EXE・依存DLLなしという意味ではない。
- WebView等の実行時依存は調査し、配布物への同梱または導入方法を整備する。インストール後のBasic機能はオフラインで動作させる。
- デスクトップ版Word・Excelへの依存は、それぞれのOffice形式からPDFへの変換だけに限定する。
- Microsoft Storeへの申請・公開、署名証明書の購入、AI・BYOK機能は今回の完成条件に含めない。Store公開は後続工程とする。
- 段階的に実装しても、以下のBasic機能を無断で完成範囲から外さない。

## 3. 実装する機能

詳細は要件定義書に従う。以下は実装漏れを防ぐためのチェックリストである。

### PDFの読み込みとページ整理

- ファイル選択とドラッグ＆ドロップによる複数PDFの取り込み。元PDFごとに出力グループを作る。
- サムネイル一覧を主画面とし、1グループ＝1つの出力PDFとする。出力名、ページ数、出力順、元ファイル名、元ページ番号を表示する。
- サムネイルサイズ調整とダブルクリックでの拡大プレビュー。
- 単一選択、Ctrlによる複数選択、Shiftによる範囲選択。
- 挿入位置を表示するドラッグによる並べ替え、グループ間移動、Ctrl＋ドラッグによるコピー。ボタンでも移動・コピーできるようにする。
- 複数ページの左右90度回転、削除、Undo/Redo。削除のたびに確認ダイアログを出さない。
- グループ全体のドラッグによる結合、ページ間の区切りによる分割、選択ページの移動・コピーによる別グループ作成、指定ページ・範囲の抽出。
- パスワード付きPDFの入力対応。非対応の保護設定は理由を表示する。

### コメント

- 常時表示する文字テキストボックスを追加・移動・サイズ変更・編集・削除できるようにする。
- 文字サイズ、文字色、背景色を指定できるようにする。
- ページ移動・コピー・回転にコメントを追従させる。
- 標準的なPDF注釈として保存し、本アプリで再度開いて編集・削除できるようにする。ページ画像への焼き付けで代替しない。
- 他の対応ビューアでの表示・印刷、既存注釈の表示・保持を検証する。日本語コメントも対象とする。

### 形式変換

- PDF→DOCX：各PDFページを画像としてWordの1ページに配置し、縦横比を維持する。Wordなしで生成する。
- PDF→XLSX：各PDFページを画像として1シートに配置し、印刷時に1シートが1ページに収まる設定にする。Excelなしで生成する。
- 画像品質は標準と高画質を用意し、初期値を標準とする。
- DOCX→PDF：デスクトップ版Wordを利用し、文書全体を変換する。
- XLSX→PDF：デスクトップ版Excelを利用し、全シートまたは選択シートを印刷設定に従って変換する。
- WordとExcelを個別に検出し、利用できない変換機能だけを無効化して理由を表示する。
- 実際の変換結果を保存前に確認できるプレビューを用意する。OfficeなしでのDOCX/XLSXプレビュー方式も技術検証に含め、元PDFの表示だけで実際の変換結果の検証を済ませない。
- Officeファイルの取り込み時はPDF変換・プレビュー・確認後にグループへ追加する。作業保存には変換済みPDFを含める。

### 書き出しと作業保存

- 全グループまたは選択グループを、グループごとに別PDFとして出力する。
- 出力名を編集でき、重複名と保存先の既存ファイルを確認する。既存ファイルはユーザー確認後に上書きする。元の入力PDFは変更しない。
- プレビューと出力の順番・向き・グループ構成・コメントを一致させる。
- 元PDFのコピーと編集情報を含む単一の作業プロジェクトファイルを保存・再開できるようにする。元のパスに依存させない。
- 異常終了に備えて自動保存し、次回起動時に復元または破棄を選べるようにする。自動保存と出力PDFの書き出しを分離する。
- 長時間処理には進捗・キャンセルを設け、個別ファイルの失敗後もほかの作業を継続できるようにする。

### 対象外

OCR、本文や画像の直接編集、編集可能な文章・セルへの復元、署名、共同レビュー、クラウド共有、独自アカウント、AI、旧Office形式、マクロ付きOffice形式、OfficeなしでのOffice→PDF代替エンジンは実装しない。その他の対象外機能は要件定義書12章に従う。

## 4. 設計と技術選定の進め方

- Rust＋Tauriを固定条件とし、互換性のある安定版を確認してバージョンを記録する。存在しないAPIや未検証のライブラリ能力を前提に設計しない。
- UIはHTML/CSS＋TypeScriptによる薄い表示・操作層を初期案とする。既存UI基盤があれば尊重する。UIをRustのみで記述することは必須としない。
- GUIのLook and FeelはKoharuに合わせる。配色、フォント、余白、角丸、アイコン、ボタン・入力欄・ダイアログの見た目と操作時の反応を、Koharuのデザインに沿って統一する。実装前に参照できるKoharuの実装・デザイン資料を確認し、参照先が不明な場合はユーザーに確認する。Koharuの外観を推測で定義しない。PDF Toolkit固有のページ整理・プレビュー操作は、本アプリの機能要件を満たすように設計する。
- Rustのドメイン処理をTauriのコマンドやPDFライブラリから分離し、ページ・グループ・コメントの編集規則を独立してテストできるようにする。
- 入力文書、元ページ、編集上のページインスタンス、出力グループに安定したIDを与える。ページのコピー後に片方のコメント編集がもう片方へ意図せず反映されないようにする。
- 座標系、ページサイズ、CropBox等、既存回転と追加回転の扱いを定義し、プレビュー・注釈編集・書き出しで共有する。
- PDFエンジンは、表示だけでなくページ操作、暗号化PDF、既存注釈保持、編集可能な文字注釈、日本語、再配布ライセンス、配布バイナリを確認して選ぶ。必要なら表示と編集のエンジンを分けるが、実ファイルで互換性を検証する。
- 主要候補の公式資料を確認し、選定理由と参照URLを `docs/technical-decisions.md` に記録する。有料ライブラリの導入はユーザーへ確認する。
- Office連携はWindows向けアダプターに分離する。専用ワーカー等でUIの応答性を保ち、自分が開始した処理だけを管理して、利用者が開いているOfficeを終了させない。
- 作業ファイルは、形式バージョン付きマニフェストとPDF実体をまとめたコンテナーを初期案とする。拡張子、暗号化PDFの格納方針、復旧形式は実装前に決定・記録する。
- 保存中断時の破損を避けるため一時ファイルと置換を用いる。キャンセル・失敗時の後始末を定義する。
- 大量ページのサムネイルは必要に応じて生成・キャッシュし、全ページの高解像度画像をメモリに常駐させない。
- IPCの入力を検証し、Tauriの権限は必要範囲に限定する。文書・パスワードをログや外部サービスへ送信しない。
- 日本語UIを初期案とする。未確定の解像度、自動保存間隔、性能評価条件は妥当な値を選び、測定結果とともに記録する。

## 5. 開発順序と段階ごとの完了条件

最初に `docs/implementation-plan.md` と `docs/requirements-matrix.md` を作成する。要件ごとに実装箇所・検証方法・状態を対応付ける。計画を作っただけで終了しない。

1. **技術検証と骨格**：開発環境を確認し、Tauriウィンドウを起動する。実PDFの表示・ページ操作・日本語注釈の保存再編集、Office連携方式、DOCX/XLSX生成とプレビュー、必要なネイティブ依存の配布方法を小さく検証する。主要要件を満たせない技術を本実装へ持ち込まない。
2. **最初の一連の操作**：実PDFを読み込み、サムネイルを表示し、並べ替え・回転して別PDFへ保存し、再度開いて結果を確認する。元PDFが変更されないことを確認する。
3. **ページ整理の完成**：複数選択、複数グループ、結合・分割・抽出・コピー、Undo/Redo、出力名と上書き確認を実装する。
4. **コメント**：追加から保存・再読み込み・再編集・削除までを実装し、回転・コピー時の座標と他ビューアの表示を確認する。
5. **作業保存と復旧**：元PDFを含む単一ファイル保存、再開、自動保存、異常終了後の復元・破棄を実装する。
6. **形式変換**：PDF→DOCX/XLSX、Officeの個別検出、DOCX/XLSX→PDF、シート選択、変換プレビューを実装する。
7. **配布と総合検証**：進捗・キャンセル・エラー処理・性能を確認し、Windows向けリリースビルドとインストーラーを作成する。開発サーバーを停止した状態で起動・主要操作を検証する。

各段階で実装と検証を行い、失敗は原因を特定して修正する。技術上の根拠があれば順序を調整してよいが、主要機能を無断で省略しない。

## 6. 検証と完成の判定

- 単体テスト：並べ替え、移動とコピーの独立性、回転、選択範囲、グループ分割、Undo/Redo、コメント座標、保存形式の往復を確認する。
- 実ファイルテスト：異なるサイズ・向き、既存回転、日本語、既存注釈、暗号化、破損したPDFを含む自作または利用許諾済みのサンプルを使う。
- 統合テスト：出力を再度開き、ページ数・順番・向き・コメントの保存と再編集を確認する。表示は別の対応ビューアでも確認する。
- DOCX/XLSXはファイルが存在するだけで合格としない。ページ・シート数、画像、縦横比、印刷設定、表示を確認する。
- Officeあり・なしの動作を区別して検証する。Officeがない環境での無効化が正常でも、Officeありの変換を検証済みとしない。
- 元PDFを別の場所へ移した後の作業再開、異常終了後の復元、キャンセル、失敗後の再操作を確認する。
- Rustのフォーマット、静的解析、テスト、UIの型検査、リリースビルドを実行し、実行したコマンドと結果を記録する。
- 配布物のインストール、起動、オフライン動作、アンインストールを検証する。可能なら開発ツールのないWindows環境でも確認する。
- 未検証事項は理由・影響・再現手順を残す。環境制約による未検証を成功として扱わない。
- 要件マトリクスでBasic版の必須要件が満たされ、実ファイルの一連の操作と配布物の起動が確認できて初めて完成とする。途中の実用ビルドは中間成果物として区別する。

## 7. 成果物と報告

- Rust＋Tauriのソース、依存関係のロックファイル、ビルド設定。
- Windows向け配布物と、その生成手順・必要な実行環境・起動方法。
- 更新した `README.md`：利用方法、開発環境、ビルド・テスト方法、Office依存範囲、既知の制約。
- `docs/technical-decisions.md`、`docs/implementation-plan.md`、`docs/requirements-matrix.md`、`docs/test-report.md`。
- 利用ライブラリと再配布物のライセンス表記。

通常の実装詳細は、根拠を記録して自律的に決めて進める。費用発生、文書の外部送信、主要機能の削減、元ファイルを失う仕様変更はユーザーへ確認する。外部サービスへの書き込みやStore公開は別途依頼がある場合に行う。

進捗報告では実装済み・検証済み・未完了を区別する。作業を中断する場合は再開地点を残す。最後に、配布物の場所、起動方法、検証結果、残っている制約を簡潔に報告する。

### 各フェーズのチャット報告

各フェーズの開始時・完了時に、チャットへ「現在の全体開発進捗％」「完了した実装と検証」「未完了・未検証事項」「次の作業」を報告する。進捗の算定基準と現在値は `docs/progress.md` に記録する。経過時間やコード量だけで進捗を増やさず、完成条件を満たした割合で算定する。100％は全必須機能と配布物の検証が完了した場合に限る。
