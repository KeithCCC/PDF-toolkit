# PDF Toolkit — 追加機能の開発プロンプト

> 作成日：2026-09-20。既存Basic版へ追加するための指示書。追加機能はまだ未実装。
> ユーザー指定：①目標サイズ指定付きPDF圧縮、②ページ番号・透かし・パスワード、③Explorer右クリックからの結合。
> 未指定の細部は本書の「推奨初期仕様」として具体化した。最新のユーザー指示を優先する。

---

あなたはPDF Toolkitの追加開発担当です。既存のRust＋Tauriアプリを拡張し、以下の3機能群を、実ファイルで動くWindowsスタンドアロンアプリとして実装・検証してください。計画・画面モックだけで完了せず、配布用インストーラーまで更新してください。

## 1. 前提と既存実装

1. `AGENTS.md`、Git状態、`01_Requirements.md`、`02_Development_Prompt.md`、`README.md`、`docs/technical-decisions.md`、`docs/requirements-matrix.md`、`docs/test-repor　t.md`、既存ソースを確認する。過去の検証記録は今回の変更後の成功証拠として流用しない。
2. 既存要件で対象外だった圧縮・ページ番号・透かしは、今回の依頼により対象へ変更する。ほかの対象外項目を自動的に追加しない。`refinement.md` は未入手であることを引き継ぎ、存在しない補足要件を推測しない。
3. Rust＋Tauriを継続する。PDF処理・出力条件・容量判定・暗号化・ファイル操作・Explorer連携の主要処理はRust側で担当する。Web UIは表示と入力を担当する。
4. GUIのLook and FeelはKoharuに合わせる。現在のライト・ダーク・システムテーマ、配色、文字、余白、角丸、ダイアログ、フォーカス表示を継承する。参照元は `C:/Development/projects/rust-text-editor/src/styles.css`。参照先が変わっている場合は既存UIと技術判断の記録を確認する。
5. 文書はローカルで処理し、Basic機能はオフラインで動作させる。クラウド送信、独自サーバー、APIキー、Officeへの新たな依存を加えない。
6. 既存のページ編集、コメント再編集、Undo/Redo、作業保存・復旧、形式変換、元入力ファイルの上書き防止を維持する。ユーザーがテスト中のアプリや未保存の作業を、ビルドの都合で勝手に終了しない。

現在の構成は `crates/core/src/` のPDF・モデル・保存・変換処理、`src-tauri/src/main.rs` のIPCと作業状態、`src/main.ts` と `src/style.css` のUI、NSIS配布。PDFium／lopdfの既存処理を読み、追加能力があると仮定せず技術検証する。

## 2. 推奨初期仕様

| 項目 | 初期仕様 |
|---|---|
| 圧縮対象 | 全出力グループまたは選択グループ。1グループ＝1PDF |
| 容量の単位 | 1MB＝1,000,000 bytes。目標は出力PDFごとに適用 |
| 容量指定 | 1MB・5MB・10MBのプリセット＋正の数値による任意指定 |
| 圧縮モード | オフ／画質優先／バランス／容量優先／目標サイズ指定。既定はオフ |
| ページ番号 | 出力グループごとに連番。初期値は1、下中央、全ページ |
| 透かし | 初期版は日本語を含む文字透かし。初期値は「社外秘」、中央、薄いグレー、45度、透明度70％ |
| パスワード | PDFを開くためのパスワード設定を必須機能とする。既定は無効 |
| Explorerメニュー | 「PDF Toolkitで結合」。複数PDFを受け取り、順番を確認してから結合 |
| Windows 11の表示先 | NSISを維持する初期案では「その他のオプションを確認」内の従来メニューを許容。最上位メニューへの対応は別途技術判断を記録 |

この表はユーザーの逐語的な指定ではなく、迷わず実装を開始するための推奨値である。通常の詳細変更は根拠を記録して進める。費用発生、文書の外部送信、主要機能の削減、元ファイルの破壊を伴う変更は独断で行わない。

## 3. PDF圧縮・目標サイズ指定

### 操作と結果表示

- 書き出し画面に圧縮設定を統合する。入力しただけで元PDFや作業中のページを変更しない。
- ページ編集・装飾を反映した圧縮前の出力サイズ、処理後の実サイズ、削減率、目標、達成／未達をPDFごとに表示する。複数入力の単純な容量合計を圧縮前サイズの代わりにしない。
- 10MB指定なら、最終保存対象のバイト数が10,000,000以下の場合だけ達成とする。小数表示の丸め、暗号化前サイズ、推定値で判定しない。
- 圧縮後の実データをプレビューする。全ページへ移動でき、変更前後を同じページ・倍率で比較できるようにする。
- 長時間処理には処理段階・試行回数・進捗・キャンセルを設ける。容量達成の見込みが不明な処理で、確定した残り時間を表示しない。

### 圧縮の方針

- まず不要オブジェクト、重複リソース、ストリーム等の可逆な最適化を検討する。その後、必要に応じて画像の再圧縮・解像度調整を行う。
- テキスト、ベクター、ページ寸法・向き、リンク、既存注釈、本アプリのコメントと再編集情報を維持する。非対応の構造は理由を示し、黙って削除しない。既存処理が保持できない構造を発見した場合も記録・是正する。
- ページ全体の画像化を通常の圧縮の代替にしない。OCRや本文の削除、ページの間引き、コメントの焼き付け・削除で容量目標を達成しない。
- マスク、透過、色空間、画像の配置変換、共有画像リソースを考慮する。単純なJPEG変換ですべての画像を処理しない。コメントのAppearanceを再圧縮して読めなくしない。
- 画質・解像度の上下限と最大試行回数を定義し、試験結果から決める。初期探索案は150／120／96／72dpi等とJPEG品質の組み合わせ、最大8候補とする。元解像度を超えて拡大しない。
- 各候補は同じ編集済み原稿から生成し、前候補への再圧縮を繰り返さない。サイズが単調に減ると仮定せず、実測した候補を比較する。目標を満たす候補の中では画質を優先する。
- 画像がほぼないPDF、既に最適化されたPDF、小さすぎる目標では達成できない場合がある。「必ず10MB以下になる」と約束しない。
- 達成できない場合は「目標未達」、最小の実測サイズ、画質設定を表示する。目標の変更・再試行・キャンセル、または未達サイズを了承した保存を選べるようにする。未達のファイルを成功扱いで自動保存しない。
- 圧縮後に増量する場合はその事実を表示し、圧縮前候補も維持する。元の入力ファイルは変更しない。

## 4. ページ番号・透かし・パスワード

### 4.1 ページ番号

- 全グループまたは指定グループに対し、適用ページ、開始番号、位置（上下×左中央右）、余白、文字サイズ、文字色を指定できる。
- 表記は少なくとも `1`、`1 / 12`、`Page 1 of 12` に対応する。
- 指定範囲の対象ページだけを連続採番する。分母はそのグループの採番対象ページ数とする。表紙を除外した場合、次の対象ページが開始番号になる。UIでこの規則を説明する。
- グループ内の最終出力順を基準とし、並べ替え・分割・結合後はプレビューと番号を再計算する。
- 回転後の見た目とCropBoxを基準に配置し、番号が欄外へ出ないようにする。異なるページサイズ・向きが混在しても動作させる。

### 4.2 透かし

- 文字列、文字サイズ、色、透明度、角度、位置、適用ページ範囲を指定できる。透明度0％＝不透明、100％＝不可視と定義し、不透明度と混同しない。
- 日本語フォントを適切なライセンスとともに使用し、別PCでも読める出力にする。
- 初期版では各対象ページに1つの文字透かしを配置する。画像ロゴ・タイル配置は今回の必須範囲には含めない。
- 元本文を置き換えず、独立した装飾として描画する。プレビュー・PDF表示・印刷に一致して反映する。

### 4.3 装飾設定の編集と保存

- ページ番号と透かしは作業内でオン／オフ、修正、削除、Undo/Redoできる。繰り返しプレビュー・書き出しをしても二重に追加しない。
- 初期仕様はグループ単位の出力設定。分割・抽出で作るグループは元設定をコピーし、結合先は結合先グループの設定を採用する。設定差がある結合では結果の適用設定を表示する。
- `.pdftk` と復旧データに設定を保存する。ページの範囲は可能な限り安定したページIDで保持し、コピー・削除・分割時の扱いを明文化する。
- 旧v1作業ファイルを読み込めるよう、形式バージョン・移行・既定値を設計する。未対応の新形式を旧アプリで開いた場合は誤って情報を落として保存させない。
- 書き出したPDFだけを再取り込みした場合の番号・透かしは、初期版では通常のPDF内容として扱ってよい。元設定の再編集は作業ファイルで保証する。既存のコメントのPDF再編集機能は維持する。
- DOCX/XLSXへの画像出力にもページ番号・透かしを反映する。PDFの圧縮・暗号化設定はDOCX/XLSXには適用せず、画面で区別する。

### 4.4 パスワード

- PDF書き出し時に「開くパスワード」を設定できる。入力確認欄、表示／非表示切替、入力不一致・未入力の検証を用意する。
- 現行ビューアとの互換性を調査したうえでAES-256を採用する。古い方式へ黙って切り替えたり、見かけの入力欄だけで保護済みと表示したりしない。
- ユーザーパスワードと所有者パスワードの区別を設計する。所有者パスワードを空や固定値にせず、利用ライブラリの要件に応じて安全な値を扱う。
- 印刷・コピー等の権限制限は任意の拡張とし、初期必須は「パスワードなしでは開けないPDF」。権限制限を追加する場合、ビューア依存の制限を絶対的なコピー防止と説明しない。
- 平文パスワードをコマンドライン引数、環境変数、ログ、例外、分析情報、作業ファイル、復旧ファイルへ残さない。原則として当該出力処理のメモリ内に限定し、再起動後は再入力する。
- 作業保存には「保護を有効にする」という設定だけを保持し、秘密値を除外する。復旧・再開後に値がない場合、保護を黙って解除して出力せず、入力を要求する。
- 保護対象は書き出すPDFである。既存の `.pdftk` と復旧データが暗号化されるわけではないことを、パスワード設定画面で明示する。作業ファイルの暗号化は今回の範囲外。
- 正しいパスワードで開けること、パスワードなし／誤入力では開けないことを、本アプリと別の対応ビューアで確認する。解除は正しい認証と編集許可の範囲内とし、解析・総当たり機能を追加しない。

## 5. 共通の出力処理

最終ページ構成を確定 → ページ番号・透かし・コメントを反映 → 圧縮候補を生成 → 必要なら暗号化 → 最終バイト数と開閉・描画を検証 → 結果を確認 → 原子的に保存、という流れを基本にする。

- 目標容量は装飾・注釈・暗号化の増分を含む最終データで判定する。これらの設定変更後は古い達成表示とプレビューを無効にする。
- プレビューで確認・容量判定した最終候補と保存するバイト列を一致させる。保存直前に無検証の再変換をしない。
- キャンセル・失敗時は入力・既存出力を保持し、一時ファイルを後始末する。平文PDFの一時ファイルが必要なら、保存先のアクセス範囲・寿命・起動時の残留清掃を設計する。
- 複数グループの処理結果と個別エラーを表示する。途中で保存済みのファイルが残る場合は、保存済み／失敗／キャンセルを明確にする。
- 保存先の既存ファイルの上書き確認、元PDF・元Office文書の上書き拒否を維持する。

## 6. Explorer右クリック → 結合

### 利用フロー

1. Explorerで複数PDFを選択して「PDF Toolkitで結合」を実行する。
2. アプリを起動、または既存インスタンスへ1件の結合依頼として渡す。
3. ファイル名・ページ数・結合順を表示する。自然順のファイル名ソートを初期案とし、ドラッグで順番を変更できる。Explorerのクリック順が取得できると仮定しない。
4. ユーザーが確認すると、1つの新しい結合グループを作成する。既存のページ編集・装飾・圧縮・パスワード設定・書き出しをそのまま利用できるようにする。

既存インスタンスに未保存の作業がある場合も内容を破棄しない。結合依頼は確認待ちとして受け取り、既存グループに無断で混ぜない。起動中・処理中の依頼はキューへ入れ、受付結果を表示する。

### Shell連携とインストーラー

- 複数選択のファイル一覧を1回の依頼として受け取る。単純な `%1` の登録だけで複数選択対応が完成したと扱わない。Shellがファイルごとに呼び出す方式を採用する場合は、確実な集約と依頼境界が必要。
- 既存の単一起動プラグインを拡張し、初回起動の引数と、起動済みインスタンスへ渡る引数の両方を同じ検証・受付経路に通す。
- 引数はPDFのパスとして扱い、シェルコマンドを組み立てて実行しない。実行ファイルの絶対パスと引数の引用を正しく扱う。空白、日本語、引用符に関わる入力、不正なオプション、長いパスを検証する。
- 多数ファイルでコマンドライン長制限に達するケースを検証する。必要なら選択一覧を安全なIPC等で渡す。無制限にパスを引数へ連結するだけの設計にしない。
- 複数の依頼にはIDを付け、同じ依頼の再配送による二重取り込みを防止する。別々の依頼を短い時間窓だけで誤って1件にまとめない。
- PDF以外、1ファイルのみ、削除済み、読取不可、破損、保護付きファイルでは理由と対象を示す。失敗ファイルを黙って除外した結合を作らず、除外する場合は一覧上で確認する。
- Shell側では受付とアプリへの引き渡しだけを行い、PDF解析・圧縮・結合をExplorerプロセス内で実行しない。
- インストール時に「Explorerに結合メニューを追加する」を選べるようにし、既定は有効を推奨する。ユーザー単位の登録を優先し、PDFの既定アプリは変更しない。
- 更新・再インストールで重複登録しない。アンインストールで自アプリ所有の登録だけを削除する。Shell拡張のロード中・ファイルロックも検証し、Explorerを無断で強制終了しない。
- Windows 11の最上位メニューと従来メニューを区別する。単純なレジストリ登録で最上位表示を保証しない。初期案の従来メニュー対応で出荷する場合は、使い方にも「その他のオプションを確認」からの操作を記載する。

## 7. 技術検証と設計の進め方

- 先に圧縮・AES-256書き出し・複数選択のShell受け渡しを小さな実ファイルで検証する。Rustライブラリのみで不足する場合は同梱ネイティブ依存を比較し、ライセンス、配布サイズ、x64ビルド、オフライン動作、キャンセル、秘密値の受け渡しを確認する。
- QPDF等の採用は検証後に判断する。オブジェクト・ストリーム圧縮と、画像の解像度変更・目標容量への探索を別の能力として評価する。CLIのオプション名だけから必要機能が揃うと判断しない。
- 新たな依存はバージョン・取得元・ハッシュを固定し、ライセンスと配布物に反映する。実行時に別途CLIを手動インストールさせない。
- 圧縮、装飾、暗号化、出力パイプライン、Shell受付は責務を分け、既存コアからテストできる構成にする。ファイル名・フォルダー名は既存構成に合わせて選ぶ。
- 実装前に `docs/additional-implementation-plan.md` と `docs/additional-requirements-matrix.md` を作成し、採用方式と推奨値からの変更理由を記録する。

公式資料の確認先（実装時にも更新を確認する）：

- [Microsoft：Explorerコンテキストメニュー統合](https://learn.microsoft.com/en-us/windows/apps/desktop/modernize/integrate-packaged-app-with-file-explorer) — Windows 11のモダンメニューに必要なIExplorerCommand・アプリIDの方式を確認する。
- [QPDF：コマンドラインオプション](https://qpdf.readthedocs.io/en/latest/qpdf-options.html) — 最適化と暗号化の実際の能力・制約を確認する。
- [QPDF：PDF暗号化](https://qpdf.readthedocs.io/en/stable/encryption.html) — ユーザー／所有者パスワードと暗号化方式を確認する。

## 8. フェーズとチャットでの進捗報告

既存Basic版の100％とは別に、今回の追加開発は0％から管理する。`docs/additional-progress.md` を作成し、過去の進捗記録を上書きしない。

| フェーズ | 完了条件 | 配点 | 累積 |
|---|---|---:|---:|
| 1. 技術検証・設計 | 圧縮・暗号化・複数選択Shell連携の実証、設計・試験計画 | 15％ | 15％ |
| 2. 圧縮・目標サイズ | 実サイズによる達成／未達、比較プレビュー、キャンセルを検証 | 25％ | 40％ |
| 3. ページ番号・透かし | 混在ページ、再編集、Undo/Redo、作業保存・移行を検証 | 20％ | 60％ |
| 4. パスワード・統合出力 | 暗号化後のサイズ判定、正誤パスワード、秘密値非永続化 | 15％ | 75％ |
| 5. Explorer結合 | 複数選択、初回／起動済み／処理中、順序確認、登録と解除 | 15％ | 90％ |
| 6. 回帰・配布 | 既存機能の回帰、release、導入・更新・アンインストール | 10％ | 100％ |

各フェーズの開始時・完了時に、チャットへ以下を報告する：

「追加開発進捗：○％／現在のフェーズ／実装済み・検証済みの内容／残っている問題・未検証事項／次の作業」

開始しただけで配点を加算しない。実装済みと実機検証済みを分け、模擬テストを実環境試験と呼ばない。100％は必須機能と配布検証が完了した場合に限る。環境依存の未検証事項は理由と影響を残す。

## 9. 必須の受け入れ試験

| 領域 | 確認するケースと合格条件 |
|---|---|
| 容量指定 | 達成可能な画像PDFで10MB以下。最終暗号化ファイルのbytesまで検査。境界値・小数・無効入力・既に目標以下も確認 |
| 未達 | 小さすぎる指定、文字中心、最適化済みPDFで未達を正しく表示し、内容を削除しない |
| 圧縮品質 | 文字検索・選択、ベクター、色、透過画像、既存注釈、日本語コメント再編集を維持。前後の同一ページを比較 |
| 装飾 | A4/A3等の混在、縦横、既存回転、非ゼロCropBox、表紙除外、順序変更、分割・結合。二重描画なし |
| 保存互換 | v1作業を読む、新設定を保存・再開・復旧する。パスワードの秘密値が保存物に存在しない |
| 暗号化 | 正しいパスワードだけで開ける。本アプリ＋独立ビューアで表示。保護付き入力、キャンセル後の一時データも確認 |
| 組み合わせ | 番号＋透かし＋コメント＋圧縮＋パスワード＋結合を同時に使って保存・再読込し、表示と最終容量が一致 |
| Explorer | 2・20・100ファイル、空白・日本語・長いパス、別の連続依頼、重複配送、不正引数。選択した全PDFが1依頼として届く |
| 既存作業 | 未保存・処理中の既存インスタンスへ依頼しても作業を失わず、依頼が重複しない |
| 配布 | 実Explorerから起動して結合、更新後もメニューが1つ、解除後は消える。開発サーバーなし、同梱依存だけで動作 |
| 回帰 | 既存のページ整理・コメント・DOCX/XLSX・Office入力・作業保存・復旧・上書き防止が動作 |

Rustのfmt/clippy/test、UI型検査、releaseビルド、実ファイル・実Explorer・インストーラー試験を実行し、コマンド・OS・試料・結果を記録する。100ページ以上の代表的なスキャンPDFで時間とピークメモリも測定し、軽量な試料だけで性能を判断しない。

## 10. 成果物と最終報告

- 追加機能を組み込んだソース、固定した依存、移行処理、ビルド・登録・解除設定。
- 更新したWindowsインストーラー、追加依存のライセンスと再現可能な取得手順。
- 利用手順を更新したREADME。目標未達の扱い、パスワードの保護範囲、Explorerメニューの表示先を説明する。
- 追加開発の計画・要件対応表・進捗、および `docs/additional-test-report.md`。
- 最終報告には、追加開発進捗％、追加した機能、配布物へのリンク、実際の試験結果、未解決・未検証の制限を簡潔に記載する。

Store公開、署名証明書の購入、OCR、画像透かし、作業ファイル自体の暗号化、パスワード解析は今回の必須範囲に含めない。
