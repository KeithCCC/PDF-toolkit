# 追加開発進捗

開始：2026-09-20。基点：`a747cfa`（origin/linkへプッシュ済み）。

全体：**25％**（2026-09-21）。圧縮・目標サイズのフェーズ2を先行完了。フェーズ1はShell複数選択の実証が残るため未加点。既存Basic版の100％とは別に管理する。

2026-09-21のコミット対象：以下の圧縮・出力GUI実装、検証スクリプトと記録。過去の「未コミット」記述は各作業時点の状態を示す。

| フェーズ | 配点 | 累積 | 状態 |
|---|---:|---:|---|
| 技術検証・設計 | 15 | 15 | 実施中 |
| 圧縮・目標サイズ | 25 | 40 | 完了（25点加算） |
| ページ番号・透かし | 20 | 60 | 未着手 |
| パスワード・統合出力 | 15 | 75 | 圧縮との統合・GUI・保護フラグ保存済み。装飾との統合待ち |
| Explorer結合 | 15 | 90 | 調査中 |
| 回帰・配布 | 10 | 100 | 未着手 |

各フェーズの完了条件をすべて満たした時点で配点を加算する。GUI接続前のコア検証を、利用可能な機能の完成とは扱わない。

「累積」は計画順にすべて完了した場合の値。現在はフェーズ2だけの25点を加算している。

## 2026-09-21：圧縮・目標サイズのGUI統合

- オフ・画質優先・バランス・容量優先・目標MB、最大8候補の探索を実装。
- 配置行列、UserUnit、共有画像、入れ子Formを考慮したDPI縮小。通常のRGB画像だけを変換し、マスク・注釈・特殊色空間等は保持。
- 全ページの同一倍率比較、実bytes、達成/未達、未達了承後の保存、古い候補の無効化、キャンセルを実アプリで検証。
- AES-256パスワード入力・確認・表示切替を接続。v1作業を移行し、v2には設定と保護フラグだけ保存。再開後は秘密値を要求。
- 通常Rustテスト41件、UI入力テスト2件、clippy/fmt、デバッグ実行ファイルのビルド成功。100ページ試験は別途明示実行して成功。
- 100ページ合成画像試料：117,259,716 → 32,106,221 bytes、約5.41秒、ピークWorking Set 632,516,608 bytes。実スキャン文書・別PCの性能保証ではない。
- 次：ページ番号・日本語透かしの設定・描画・Undo/Redoと範囲ID。Explorerの実複数選択連携と新インストーラーは未完了。
- 軽微な既知事項：同じ出力設定を再適用してもUndo履歴が1件増える。レビューの重要指摘3件は修正し再現テストを通過。
- テスト用アプリは `target/debug/pdf-toolkit.exe`。今回の検証ビルドは通常版とは別のアプリIDを使用。リリース版・インストーラーはBasic版のまま。

## 2026-09-20：最初のコア実装

- 目標MBの正確な変換、暗号化後の容量判定、達成候補の品質優先選択、未達候補の識別を実装。
- 不要オブジェクトの削除・ストリーム圧縮と、通常の8bit RGB画像のJPEG再圧縮を実装。本文と注釈を保持し、マスク等の対象外画像は理由付きで保持。
- AES-256 R6による開くパスワードを実装。日本語、誤認証、空へ正規化されるパスワードの拒否を検証。
- Rustテスト28件、clippy、fmt、Tauriのcargo checkが成功。独立したMuPDFでも文字・日本語注釈・認証を確認。
- 次：画像配置を考慮したDPI調整、候補探索、Explorer複数選択受け取り方式の実証。その後、装飾・作業形式・GUIへ統合。
- 詳細は `additional-test-report.md`。追加コードはローカル変更であり、基点コミットには含まれない。
