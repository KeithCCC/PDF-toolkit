# Work log

## 2026-09-20 — Basic版と追加開発プロンプトの保存

- Branch: `link`
- Rust＋TauriによるPDF整理、注釈、作業保存・復旧、DOCX/XLSX変換、Office入力を実装。Koharu準拠の日本語UIとNSIS配布を追加。
- 依存ロック、PDFium取得・ハッシュ検証、同梱ライセンス、試験スクリプト、要件対応と検証記録を保存。
- 圧縮・容量指定、ページ番号・透かし・パスワード、Explorer結合の追加開発プロンプトを作成。追加実装はこのコミット後に開始する。
- 検証：Basic版のRustテスト14件、fmt/clippy、TypeScript、release/NSIS、導入・起動・アンインストールは `docs/test-report.md` に記録。コミット前にRustテストとフロントエンドビルドを再実行。
- 制限：Office未導入の別OS、実プリンターなどの環境互換試験は未実施。配布バイナリ・生成出力・取得DLLは除外し、再生成手順をGit管理する。

## 2026-09-20 — コミット・プッシュ後の追加開発開始

- `a747cfa` を `origin/link` へプッシュ済み。
- 追加実装計画・進捗・要件対応表を作成し、容量判定、非破壊圧縮の初期処理、AES-256暗号化をRustコアへ追加。
- Rustテスト28件、clippy、fmt、Tauri check成功。PDFiumとMuPDFで暗号化PDFを検証。
- 追加コードはローカルで未コミット。GUI・装飾・Explorer連携は後続作業。詳細は `additional-progress.md` と `additional-test-report.md`。

## 2026-09-21 — 圧縮フェーズのGUI統合

- 画像配置に基づくDPI縮小、最大8候補探索、暗号化後の容量判定と保存までを実装。全ページ比較・未達了承・キャンセル・候補無効化を接続。
- グループの出力設定と保護フラグをv2作業へ保存、v1移行と秘密値非保存を検証。番号・透かしに先行して出力UIを統合。
- Rust41件、UI入力2件、実Tauri操作、独立MuPDF、100ページ性能、fmt/clippy/buildを検証。レビュー指摘とPDFium並列問題は再現後に修正。
- フェーズ2の25点を加算し全体25％。Shell実証・装飾・全機能の統合・配布は未完了。変更は未コミット。

## 2026-09-21 00:47 JST — 追加開発25％時点のコミット・プッシュ

- Branch: `link`。PDF圧縮・目標サイズ・AES-256保護・全ページ比較・確認保存を追加したソース、v2作業保存とv1移行、検証スクリプト・進捗記録をまとめて保存。
- 検証：直前の開発でRust41件、UI入力2件、fmt/clippy/build、実Tauri操作、独立MuPDF、100ページ性能試験が成功。今回の保存では差分と検証ログ、ステージ内容を確認。
- 出力候補の無効化、キャンセル、注釈・マスク保持、PDFium並列処理の修正を含む。生成PDF・実行ファイル・取得DLLはGit対象外。
- 後続：ページ番号・透かし、Explorer結合、全機能統合とインストーラー。軽微な既知事項は同一出力設定の再適用でもUndo履歴が増えること。追加開発進捗は25％。
